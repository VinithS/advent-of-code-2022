pub mod day1;

use std::env;
use std::fs;

// Calorie Counting
fn day_one() {
    println("Hello World!");

    let file_path = "./lib/day-1.txt";

    let contents = fs::read_to_string(file_path).expect("should be able to read the file");

    println!("W")

    // number of calories each elf is carrying
}
